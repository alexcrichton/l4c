//test return 5
// Test that typedefs can be used

typedef int haha;
typedef bool heehee;

int main()
{
    int x = 1;
    haha y = 2;
    int z = x + y;

    bool a = true;
    heehee b = false;

    if(a && b == true) return z;

    for(x = 1; x < 10; x++)
    {
        haha u = z + x;
        y = u + 1;
        break;
    }

    return y;
}
