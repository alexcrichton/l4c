//test return 100

int main(){
	int x = 0;
	int y = 1;
	int z = 2;
	if(x % y == z)
		x += 32;
	else if(y == x || x + 2 >= z || 0 / 0 == 1){
		for(y = 100; y > 0; y--)
			x += y % 2;
		for(z = 5; z > y; y++){
			int i;
			x + y - z % x;
			for(i = z; i > 0; i--)
				x <<= i;
			while(i < 42){
				i += 5;
				if(i % 2 == 0) continue;
				else if(i % 3 == 0) x /= i;
				else x -= i;
			}
		}
	}else
		x += 42;
	return x + 2158857;
}
