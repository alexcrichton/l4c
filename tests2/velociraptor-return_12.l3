//test return 1
// recursive calls- it does end eventually! Are you dealing with stack
// space correctly?

int recurse(int x) {
  if (x == 0) {
    return 1;
  }
  else {
    return recurse(x - 1);
  }
}

int main () {
    return recurse(200);
}
