//test error
// Types of arguments in function arg list don't match declaration

int get_max(int a, bool b);

int main()
{
    return get_max(0x12345, 12345);
}

int get_max(int x, int y)
{
    if(x > y) return x;
    else return y;
}
