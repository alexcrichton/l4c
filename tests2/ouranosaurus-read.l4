//test exception 11

// (from wikipedia)
int assign(int *dest, int *src) 
{
  *dest = *src;
  return *dest;
}

int main() {
    int *p = NULL;

    int *x = alloc(int);
    *x = 5;
    return assign(x, p);
}
