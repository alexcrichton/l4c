#[link(name = "l4c")];
#[deny(implicit_copies)];
#[deny(non_implicitly_copyable_typarams)];

extern mod std;

mod l4c;

mod syntax {
  mod ast;
  mod pp;
  mod parse;
  mod symbol;
}
mod util {
  mod mark;
  mod profile;
}

use syntax::*;
use util::*;
use util::profile::*;
