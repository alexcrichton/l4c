#[link(name = "l4c")];
#[deny(implicit_copies)];
#[deny(non_implicitly_copyable_typarams)];

extern mod std;

mod l4c;

use syntax::*;
use util::*;
use util::profile::*;

mod syntax {
  mod ast;
  mod pp;
  mod parse;
  mod symbol;
  mod error;
  mod mark;
}
mod util {
  mod profile;
}
