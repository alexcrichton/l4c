#[link(name = "l4c")];
#[deny(implicit_copies)];

extern mod std;

mod l4c;

mod syntax {
  mod ast;
  mod pp;
  mod parse;
}
mod util {
  mod symbol;
  mod mark;
  mod profile;
}

use syntax::*;
use util::*;
use util::profile::*;
