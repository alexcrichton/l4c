//test return 201
// Function decleration, type checking
int plus (int x, int y);
int plus (int x, int y);
int plus (int x, int y);

int sub (int x, int y);
int sub (int x, int y);

typedef int is_a_int;

int plus (int x, int y) 
{
  return x + y;
}

int main () 
{
  int x;
  is_a_int y;

  x = 100;
  y = 100;

  return (booltoint(inttobool(y)) + plus(x,y));
}
