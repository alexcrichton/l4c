//test return 4
// Arrays of length 0 ARE allowed, you just can't do anything to them!

int blah(int[] n, int i) {
	if (i > 0) {
		return n[0];
	}
	else {
		return 4;
	}
}

int main() {
	int[] n = alloc_array(int, 0);
	return blah(n, 0);
}
