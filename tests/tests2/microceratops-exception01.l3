//test exception
// 

bool ret_true(int x);

bool ret_true(int x)
{
  bool ret_true = true;
  int hundred = 100;
  
  while(ret_true) {
    ret_true = inttobool(hundred);
    hundred--;
  }

  return !ret_true;
}

int main () {
  while(ret_true(0)) {
    
  }
  return 0;
}
