//test error
// Because we've totally done this in the past.

int g(int x) {
  return f(x);
}

int f(int x) {
  return g(x);
}

int main() {
  int iwishicouldmutuallyrecursewithoutprototypes = 0;
  return f(iwishicouldmutuallyrecursewithoutprototypes);
}
