//test return 0
// takes a decimal number and prints the binary value

int dec2bin(int num)
{
    if (num == 0)
        return num;
    dec2bin(num / 2);
    printint(num % 2);
    return num;
}

int main()
{
    dec2bin(1337);
    printchar(0xa);
    return 0;
}
