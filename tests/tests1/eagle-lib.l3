//test return 4

int main() {
  if (readint() != 3) return 0;
  if (readhex() != 0xdeadf00d) return 0;
  
  int k = readfloat();
  int j = readfloat();
  k = fadd(k, j);
  k = fromfloat(k);
  return k;
}
