structure AllocationTest :> TESTCASE =
struct

  open TestUtils
  structure A = Assem

  (*fun equals    []       []    = true
    | equals (i1::I1) (i2::I2) = 

  fun test_add () =
    equals (Allocation.allocate [add(tmp(), tmp(), tmp())]) [add(A.ECX, A.EAX, A.EBX)]*)

  val tests = []
end
