//test return 50

// compute the product 5 * 10 = 50
int main(){
	int n = 100;
	int i;
	int s = 0;
	for(i = 0; i < n; i++){
		if(i % 2 == 0) continue; // we only work with odd numbers
		if(i * 2 == 42) break;
		s += 5;
	}
	return s;
}
