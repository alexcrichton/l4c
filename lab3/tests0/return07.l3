//test return 42
// using break and continue

int div (int x, int y) {
  int q;
  q = 0;
  while (true) {
    if (x < y) break;
    q += 1;
    x -= y;
    continue;
    x -= y;
  }
  return q;
}

int main () {
  return div(127,3);
}
