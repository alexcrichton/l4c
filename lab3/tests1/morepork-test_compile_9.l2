//test error

/* 
 * This is my test program
 * Using malformed continue
 */
int main () {
  int x;
  int y = 15;
  ( y ) = x;
  x = 2 + 7;
  for (x = 0; x < 5; x = x + 1)
  {
    continue(0);
    y = -y;
  }
  x += 1;
  //should return 4
  return ( x + y);
}
