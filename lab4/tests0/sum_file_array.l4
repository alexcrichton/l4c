//test return 55

int fail() {
	return 1 / 0;
}

int main() {
	int[] a = alloc_array(int, 10);
	for (int i = 0; i < 10; i++) {
		a[i] = readint();
	}
	if (!eof()) {
		fail();
	}

	int sum = 0;
	for (int i = 0; i < 10; i++) {
		sum += a[i];
	}
	return sum;
}

