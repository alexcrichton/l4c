//test return 100
// Function decleration, different names, type checking
int plus (int x, int y);
int plus (int x1, int y1);
int plus (int yy, int xx);

int sub (int x, int y);
int sub (int x, int y);

typedef int is_a_int;

int plus (int x, int y) 
{
  return x + y;
}

int main () 
{
  int x;
  is_a_int y;

  x = 100;

  return plus(x,0);
}
