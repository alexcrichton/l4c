//test return 3
typedef int null;

// namespace for x
null x(null a, null b){ return a / b; }
// x is the argument and not the function here
null i(int x){
	return x;
}

int main(){
  // override function by var
  int x = 100;
	while(i(x) > 5) x >>= 1;
	return i(x);
}
