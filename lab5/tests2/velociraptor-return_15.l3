//test return 1

bool pass(int grade) {
  return (grade > 59);
}

int main () {
  int g = 80;
  int h = 32;
  int i = 60;
  if (pass(g) && pass(i) || pass(h)) {
    return 1;
  }
  else {
    return 0;
  }
}
