//test return 36
// passing more arguments than available registers
int sum(int x1, int x2, int x3, int x4, int x5, int x6, int x7, int x8, int y, int z, int w, int v){
	return x1 + x2 + x3 + x4 + x5 + x6 + x7 + x8;
}

int main(){
	int x = 1;
	int y = 2;
	int z = 3;
	// sum of 1, 2, 3, 4, 5, 6, 7, 8 = 8*9/2
	return sum(x, y, z, z + x, z + y, z + z, z + z + x, z + z + y, 0, 0, 0, 0);


	x / (y / z);
}
