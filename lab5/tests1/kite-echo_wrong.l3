//test error
//Extra argument in reachar
//I actually made this mistake whe writing echo

int main() {
  int x = readchar();
  while(x != 10) {
    printchar(x);
    x = readchar(x);
  } 
  printchar(10);
  return 0;
}
