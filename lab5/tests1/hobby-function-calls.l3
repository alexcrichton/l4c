//test return 100

bool even(int x) {
    return x % 2 == 0;
}

int main() {
    int sumodds = 0;
    for (int i = 0; i < 20; i++) {
        if (!even(i))
            sumodds += i;
    }
    return sumodds;
}
