//test return 0
//simple sanity check for generating better if statements

int main()
{
  int x = 0;
  bool b = true;
  int[] a = alloc_array(int, 5);
  a[0] = 4;
  a[1] = 1;
  a[2] = 9;
  a[3] = 7;
  a[4] = 3;
  
  if(true == false)
    x--;
  else if(a[0] >= a[3] || a[1] <= a[4])
    x++;
  else
    x+=4;
  if(b && (9 <3 && 6 > 4))
    x++;
  else
    x--;
  return x;
}